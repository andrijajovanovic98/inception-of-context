# Part 2: Architect API and RAG Dependencies

# Core Part 1 Stack (Inherited)
chromadb>=0.4.22
sentence-transformers>=2.2.2
watchdog>=4.0.0
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0

# Part 2 Specific Dependencies

# In-memory BM25 Re-ranker (Subject explicit recommendation: rank_bm25)
# Used to re-rank vector search results and handle exact identifier matches
rank-bm25>=0.2.2

# Async HTTP client for communicating with local Ollama runtime API (100% local-only)
httpx>=0.27.0
